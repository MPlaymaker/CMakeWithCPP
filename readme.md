Setup:

On Linux:
1. mkdir build
2. cd build
3. cmake ..
   This will setup the build based on the parent directory
4. cmake --build .
This will build and create and executable

On windows:
1. mkdir build
2. cd build
3. cmake ..
   This will setup the build based on the parent directory
4. cmake --build 
This will build and create and executable
